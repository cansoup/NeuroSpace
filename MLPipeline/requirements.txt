# requirements.txt
# EEG Motor Imagery Classification Pipeline
# VisionStudio Team 5
#
# Install:
#   pip install -r requirements.txt
#
# Python 3.10+ required

torch>=2.0.0          # EEGNet model + training
numpy>=1.24.0         # array ops
scipy>=1.10.0         # bandpass filter (butter / sosfiltfilt), eigh for CSP
pylsl>=1.16.0         # LSL stream I/O  (liblsl must also be installed)
                      # macOS:  brew install labstreaminglayer/tap/lsl
                      # Linux:  apt install liblsl  OR  pip install liblsl
